import React from "react";
import { MovieApp } from "../codepen/MovieApp";

const MiniProjects = () => {
  return (
    <main className="flex justify-center items-center mx-auto container min-h-screen">
      <MovieApp />
    </main>
  );
};

export default MiniProjects;
